{% extends 'base-backend.html.twig' %}

{% block content %}
    <h1>Faculties</h1>

    <ul class="list-unstyled">
        <li>
            <a href="{{ path('admin_faculty_new') }}">
                Add new faculty.
            </a>
        </li>
        <li>
            <a href="{{ path('admin_faculty_export_list_to_csv') }}">Export to *.csv.</a>
        </li>
        <li>
            <a href="{{ path('admin_faculty_import_list_from_csv') }}">Import from *.csv.</a>
        </li>
    </ul>
    <table class="table">
        <thead>
        <tr>
            <th>Name</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.name }}</td>
                <td>
                    <ul class="list-unstyled">
                        <li>
                            <a href="{{ path('admin_faculty_show', { 'id': entity.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_faculty_edit', { 'id': entity.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
